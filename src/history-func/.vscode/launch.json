{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to History Azure Function",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:azureFunctions.pickProcess}"
    }
  ]
}